import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.itrend.condor',
  appName: 'ItrendCL Cóndor',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
